import { useNavigate } from 'react-router-dom';
export default function Attadance() {
    // const Navigate = useNavigate();
    return (
        <>
        <h1>HELLOW WORlD</h1>
            {/* <div onClick={() => {
                Navigate('/Attadancemain');
            }}>CLASS-A</div>
            <div onClick={() => {
                Navigate('/Attadancemain');
            }}>CLASS-B</div> */}
        </>
    );
}